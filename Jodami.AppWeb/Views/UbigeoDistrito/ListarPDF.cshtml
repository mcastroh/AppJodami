@model IEnumerable<Jodami.AppWeb.Models.ViewModels.VMUbigeoDistrito>

@{
    Layout = null;
    var dptoText = $"Departamento: {Model.FirstOrDefault().UbiDptoCodigo} - {Model.FirstOrDefault().UbiDptoName}";
    var provText = $"Provincia: {Model.FirstOrDefault().UbiProvCodigo} - {Model.FirstOrDefault().UbiProvName}";
}

<!DOCTYPE html>

<html> 

<head>       
    <partial name="_ReporteEncabezado" />
    <style>
        body {
            font-family: Arial, Arial, Helvetica, sans-serif;
        } 
    </style>
</head> 

<body>
    <div class="contenedor">
        <table class="tbproductos table table-sm table-bordered table-striped" id="tabla" width="100%" cellspacing="0">
            <thead class="table-dark">
                <tr></tr>               
                <tr>
                    <th colspan="2" align="left"><h2>@dptoText</h2></th>
                </tr>
                <tr>
                    <th colspan="2" align="left"><h2>@provText</h2></th>
                </tr>
                <tr></tr>
                <tr style="text-align: left">
                    <th align="center" style="width:30px" scope="col">Código</th>
                    <th class="tbth" style="width:150px">Distrito</th>
                </tr>
                <tr></tr>
            </thead>
            <tbody>                
                @{
                    foreach (var item in Model)
                    {
                        string keyDist = item.CodigoDistrito.Substring(4, 2);                    

                        <tr>

                            <td align="center">@Html.DisplayFor(m => @keyDist)</td>
                            <td align="left">@Html.DisplayFor(m => item.Distrito1)</td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</body>
</html> 