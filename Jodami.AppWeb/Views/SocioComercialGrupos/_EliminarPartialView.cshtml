@model Jodami.AppWeb.Models.ViewModels.VMSocios

<div class="modal fade" id="EliminarEntidad-@Model.IdSocio" aria-labelledby="EliminarEntidadLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">   
            <div class="modal-header">
                <h2 class="modal-title">Eliminar Grupo Económico</h2>
                <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">x</span>
                </button>
            </div> 
            <div class="modal-body">                  
                <form asp-action="Eliminar" method="post"> 
                    <input type="hidden" asp-for="@Model.IdSocio" />
                    <div class="row">
                        <div class="col-sm-6 form-group">
                            <label class="control-label">Tipo documento de identidad</label>
                            <select id="IdFlete" class="form-control" asp-for="@Model.IdTipoDcmtoIdentidad" asp-items="@(new SelectList(@Model.TiposDcmtoIdentidad, "IdTipoDcmtoIdentidad", "Simbolo") ) ">
                                <option value="">Seleccionar</option>
                            </select>
                            <div class="invalid-feedback">Tipo documento de identidad obligatorio</div>
                        </div>
                        <div class="col-sm-6 form-group">
                            <label asp-for="@Model.NumeroDcmtoIdentidad" class="control-label">Número dcmto identidad</label>
                            <input asp-for="@Model.NumeroDcmtoIdentidad" class="form-control" readonly="readonly" />
                            <div class="invalid-feedback">Número documento de identidad obligatorio</div>
                        </div>
                    </div>
                    <div class="form-group">
                        <label asp-for="@Model.RazonSocial" class="control-label">Nombre del grupo</label>
                        <input asp-for="@Model.RazonSocial" class="form-control" readonly="readonly" />
                        <div class="invalid-feedback">Nombre del grupo obligatorio</div>
                    </div>
                    <div class="row">
                        <div class="col-sm-6 form-group">
                            <label asp-for="@Model.Telefono" class="control-label">Teléfono</label>
                            <input asp-for="@Model.Telefono" class="form-control" readonly="readonly" />
                        </div>
                        <div class="col-sm-6 form-group">
                            <label asp-for="@Model.Celular" class="control-label">Celular</label>
                            <input asp-for="@Model.Celular" class="form-control" readonly="readonly" />
                        </div>
                    </div>

                    <div class="form-group">
                        <label asp-for="@Model.PaginaWeb" class="control-label">Página Web</label>
                        <input asp-for="@Model.PaginaWeb" class="form-control" readonly="readonly" />
                    </div>
                                               
                    <div class="form-group">
                        <label asp-for="EsActivo" class="control-label">Estado</label>
                        @{
                            if (@Model.EsActivo)
                            {
                                <input class="form-control" value="Activo" readonly="readonly" />                                
                            }
                            else
                            {
                                <input class="form-control" value="Inactivo" readonly="readonly" />
                            }
                        }                     
                    </div>
                    <br /> 
                    <div class="modal-footer">                      
                        <button type="submit" class="btn btn-primary"> Eliminar</button>
                        <button class="btn btn-danger" data-dismiss="modal"> Cancelar</button>
                    </div>
                </form>                       
            </div>
        </div>
    </div>   
</div>  