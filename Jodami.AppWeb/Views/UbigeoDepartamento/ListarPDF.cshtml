@model IEnumerable<Jodami.AppWeb.Models.ViewModels.VMUbigeoDepartamento>

@{
    Layout = null;
}

<!DOCTYPE html>

<html> 

<head>       
    <partial name="_ReporteEncabezado" />
    <style>
        body {
            font-family: Arial, Arial, Helvetica, sans-serif;
        } 
    </style>
</head> 

<body>   
    <div class="contenedor"> 
        <table class="tbproductos table table-sm table-bordered table-striped" id="tabla" width="100%" cellspacing="0">      
            <thead class="table-dark">
                <tr style="text-align: left">
                    <th align="center" style="width:30px" scope="col">Código</th>
                    <th class="tbth" style="width:150px">Departamento</th>
                </tr>
            </thead>
            <tbody>
                @{
                    foreach (var item in Model)
                    {
                        <tr>
                            <td align="center">@Html.DisplayFor(m => item.CodigoDepartamento)</td>
                            <td align="left">@Html.DisplayFor(m => item.Departamento1)</td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div> 
</body>
</html> 